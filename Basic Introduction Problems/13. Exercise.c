

#include <stdio.h>

int main() {
    // Declare variables of type double and boolean
    double doubleValue;
    _Bool booleanValue;

    // Initialize the variables with values
    doubleValue = 3.14;
    booleanValue = 1; // In C, 1 represents true

    // Print the values
    printf("The double value: %e\n", doubleValue);
    printf("The boolean value: %d\n", booleanValue);

    return 0;
}
