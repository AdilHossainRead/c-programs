#include<stdio.h>
void print_function(int n)
{
    if(n==0)
    {
        return;
    }
    else
    {
        printf(" %d",n);
        print_function(n-1);
    }
}
int main()
{
    int N;
    printf("Enter number N=");
    scanf("%d",&N);
    print_function(N);
}
