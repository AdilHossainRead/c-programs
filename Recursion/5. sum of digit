
#include<stdio.h>
int sum_of_digit(int X)
{
    if(X==0)
    {
        return 0;
    }
    else
    {
       return X%10+sum_of_digit(X/10);
    }
}
int main()
{
    int N;
    printf("Enter number N=");
    scanf("%d",&N);

    printf("Sum of digit is : %d",sum_of_digit(N));
}


